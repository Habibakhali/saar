import 'package:flutter/material.dart';

class Belux extends StatelessWidget {
static const String routeName='Belux';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Belux'),
        ),
        body: Center(
          child: Text('Belux'),
        )
    );
  }
}
