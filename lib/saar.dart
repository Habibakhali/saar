import 'package:flutter/material.dart';

class Saar extends StatelessWidget {
static const String routeName='saar';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('ســـار'),
        ),
        body: Center(
          child: Text('saar'),
        )
    );
  }
}
